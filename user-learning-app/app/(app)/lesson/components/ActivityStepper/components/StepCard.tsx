import { YStack } from '@voxify/design_system/layout';
import { styled } from 'tamagui';

export const StepCard = styled(YStack, {
  name: 'StepCard',
  borderRadius: '$6',
  backgroundColor: 'white',
  elevation: 1,
  overflow: 'hidden',
});
